 version: 2
 jobs:
   build:
     docker:
       - image: microsoft/aspnetcore-build:2.0
     steps:
       - checkout
       - run: 
           name: Restore packages
           command:
             dotnet restore src/
       - run: 
           name: Build App
           command:
             dotnet build src/